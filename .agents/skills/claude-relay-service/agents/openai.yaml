interface:
  display_name: "Claude Relay Service"
  short_description: "Repo-specific patterns and workflows for claude-relay-service"
  default_prompt: "Use the claude-relay-service repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true